{
  # SchemaStore provider for jsonls and yamlls.
  # SchemaStore is a catalog of known json and yaml config files.
  plugins.schemastore = {
    enable = true;
    yaml.enable = true;
    json.enable = false;
  };
}
